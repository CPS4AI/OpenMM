# Build output
build/
build_cuot*/
build_*/

# Third-party dependency clones (managed by scripts/build-deps.sh, NOT project
# source — do not commit). deps/cuOT is a vendored candidate backend; its local
# M2-T2 bug fixes are documented in docs/research/m2-cuot-standalone.md §7.3
# (exact file:line + fix), not carried in this repo's git.
deps/

# Ferret / cuOT runtime cache artifacts (regenerated by test runs; the CPU
# emp-ot *_alice/_bob caches are tracked separately as reference artifacts).
data/pre_ot_data_reg_send
data/pre_ot_data_reg_recv

# editor / OS cruft
*.swp
.DS_Store
